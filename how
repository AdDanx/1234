echo $XDG_CURRENT_DESKTOP

apt install iptables-persistent
iptables -t nat -A POSTROUTING -s 10.8.0.0/24 -o ens3 -j MASQUERADE
netfilter-persistent save

brctl

apt-get install bridge-utils
brctl addbr br0
brctl stp br0 on
ip link set ens7 down
ip link set ens8 down 
brctl addif br0 ens7
brctl addif br0 ens8

далее включаем ens7-8 и br0 и идем в /etc/network/interfaces
auto ens7
iface ens7 inet static
address 0.0.0.0

auto ens8
iface ens8 inet static
address 0.0.0.0

auto br0
iface br0 inet static
bridges_ports ens7 ens8
bridges_stp on
address 203.0.113.1
netmask 255.255.255.0





Если не заработает ospf (на fw делаем)
Ip route 192.168.0.0 255.255.0.0 10.1.2.2
Ip route 192.168.0.0 255.255.0.0 10.1.3.2
!
Делаем NAT на FW
iptables -t nat -A POSTROUTING -s 203.0.113.0/24 -o gre100 -j MASQUERADE
iptables -t nat -A POSTROUTING -s 203.0.113.0/24 -o gre200 -j MASQUERADE
